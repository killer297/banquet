[project]
name = "banquet-backend"
version = "0.1.0"
description = "Banquet Management System Backend"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "flask-cors",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "backend.server:app"
